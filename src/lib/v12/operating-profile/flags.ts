/**
 * Part 5 / Release 5C AI company-setup interview (`/v12/activation`).
 * Disabled while the long industrial questionnaire is retired pending redesign.
 * Flip to `true` to re-enable the wizard in `wizard.tsx` and `startCompanySetup`.
 */
export const COMPANY_SETUP_INTERVIEW_ENABLED = false;
