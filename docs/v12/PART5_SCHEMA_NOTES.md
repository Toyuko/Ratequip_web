# Database Schema Notes

**Repository:** RateQuip Enterprise Master Repository V12.2 - Add-On 02 / Part 5  
**Generated:** 2026-07-19T17:29:38+00:00  
**Status:** Delta-only, install after V12 Part 3 and V12.1 Add-On 01 / Part 4

The add-on creates 65 source tables across three schemas. UUIDs are generated by `gen_random_uuid()`. All company-scoped tables use RLS. Source evidence remains immutable by service policy; updates create new revisions. Production deployment must add foreign keys to the exact baseline identity/project/document/supplier tables after resolving environment-specific schema names through the installation adapter.

**Install fix (web app):** `0025_project_ecosystem_work_packages.sql` had duplicate `project_id` columns (common header + domain fields). The web copy removes the redundant second declaration so Postgres can apply the migration.
