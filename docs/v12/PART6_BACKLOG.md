# Part 6 (AI Catalogue Product Factory) Prioritised Backlog

Module 68 / Features provisional **186–190** for thin slice. Canonical Part 6 migrations `0001–0006` remapped to app **`0035–0040`** under schema `catalog_factory`.

## Release 6A — turn my catalogue into drafts
Upload/paste → rights attestation → free preflight → credit estimate (Part 4 preview) → stub extraction → accept/reject drafts → publish blocked until confirmed.

**Status in web app:** `/v12/catalogue-factory`.

## Release 6B — real extraction workers
OCR, layout, table/image extraction, live LLM adapters.

## Release 6C — publish & search
Approval snapshots, search index, Ask Catalogue, Part 5 opportunity matching.
