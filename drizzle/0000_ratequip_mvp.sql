/**
 * RateQuip MVP — SQL reference migration.
 * Prefer `npm run db:push` / Drizzle Kit with DATABASE_URL set.
 * Future v10 tables: see comments in src/lib/db/schema.ts
 */

-- Generated conceptually from src/lib/db/schema.ts
-- Run via drizzle-kit against Neon Postgres.
